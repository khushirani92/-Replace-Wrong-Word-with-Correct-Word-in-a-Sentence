# Replace Wrong Word with Correct Word in a Sentence

## 📌 Project Description
This project demonstrates how to replace a wrong word with a correct word in a sentence using JavaScript `string.replace()` method inside a function.

---

## 🧠 Problem Statement
Create a function:
